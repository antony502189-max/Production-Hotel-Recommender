services:
  recommender-api:
    build: .
    ports:
      - "8000:8000"
    environment:
      APP_ENV: production
      MODEL_PATH: /app/artifacts/recommender.joblib
      CANDIDATE_POOL_SIZE: 100
      LOG_LEVEL: INFO
    restart: unless-stopped
